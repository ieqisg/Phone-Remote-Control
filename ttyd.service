[Unit]
Description=ttyd Terminal
After=network.target

[Service]
Type=simple
User=marc
ExecStart=/usr/bin/ttyd -p 8080 --writable -c user:pass bash
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
